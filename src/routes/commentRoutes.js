// import Router from 'express';
// import asyncErrorHandler from '../utils/asyncErrorHandler';
// import commentContoller from '../controllers/commentController';
// import verifyToken from '../middlewares/tokenValidation';

// const router = Router();

// router.post('/comment/:blogId', verifyToken, asyncErrorHandler(commentContoller.postingComment))
// router.put('/comment/:id', verifyToken, asyncErrorHandler(commentContoller.putComment))
// router.delete('/comment/:id', verifyToken, asyncErrorHandler(commentContoller.deleteComment))

// export default router
